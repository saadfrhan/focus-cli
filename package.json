{
  "type": "module",
  "scripts": {
    "build": "tsup src/index.ts --format esm --dts",
    "lint": "tsc",
    "test": "vitest"
  },
  "devDependencies": {
    "@types/node": "^24.3.0",
    "tsup": "^8.5.0",
    "typescript": "^5.9.2"
  },
  "dependencies": {
    "commander": "^14.0.0",
    "vitest": "^3.2.4"
  }
}
